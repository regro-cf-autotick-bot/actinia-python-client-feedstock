{% set name = "actinia-python-client" %}
{% set version = "0.3.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/actinia-org/actinia-python-client/archive/{{ version }}.tar.gz
  sha256: f95ae096b2926ad18f0cf648214c4d3d58d58e32504d08a38e872026c74513a5

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - python >=3.8
    - setuptools-git-versioning
    - pip
  run:
    - python >=3.8
    - jinja2
    - requests
    - sphinx-material

test:
  imports:
    - actinia
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/actinia-org/actinia-python-client
  summary: Python Client Library for actinia
  license: GPL-3.0-or-later
  license_file: LICENSE

extra:
  recipe-maintainers:
    - anikaweinmann
    - neteler
    - giswqs
